---
title: "Examples"
classes: wide
permalink: /examples/
---

`Clipper` now works with more than a single hosted model. If your provider exposes an OpenAI-compatible API, you can keep the same workflows while choosing the model through your configuration, including setups routed through OpenRouter.

## CLI mode

`Clipper` can be used in `CLI` mode to streamline your terminal workflow.

That means the same command-generation and chat flows can now be powered by the model you prefer for speed, price, or reasoning quality.

## REPL mode

`Clipper` can be used in `REPL` mode to offer interactive prompts and chain instructions.

In practice, this makes it easy to keep one familiar terminal assistant experience while swapping the backend model behind it.

## Example Use Cases

### Data Analysis & Querying
Analyze the users.json file and show me the active users
Parse the employees.csv and calculate average salary by department
Extract all email addresses from the special_characters.txt file

### OS & System Tasks
List all processes listening on port 8080
Show the disk usage of Docker resources
Find files larger than 100MB in /var/log
Check open network connections and their owning processes
Generate a shell command to clean up old log files

### Code & Configuration Tools Use Review
Review the sample_script.py for potential improvements
Check the sample_script.sh for security issues to fix problems
Convert the sample_config.ini to YAML format

### Log Analysis
Check the application.log for errors in the last hour
Count occurrences of each log level in the application.log

### Documentation & Text Processing
Summarize the large_document.txt
Extract all code blocks from sample_docs.md

### Database & Infrastructure
Explain what the docker-compose.yml does
Generate sample data for the tables in sample_schema.sql

### Special Content Handling
Parse special_characters.txt and identify all Unicode characters
Find all emojis in special_characters.txt

